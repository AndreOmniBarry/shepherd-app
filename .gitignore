# Environment variables - NEVER commit these
.env
.env.local
.env.production
.env.development

# Dependencies
node_modules/

# Next.js build output
.next/
out/
tsconfig.tsbuildinfo

# OS files
.DS_Store

# Claude Code local tooling state (isolated-worktree agent scratch dirs) —
# except .claude/skills/, which holds committed, shareable skills (e.g.
# run-shepherd-app, the build/run/screenshot harness) meant to be checked
# in and reused by future sessions, not local scratch state.
.claude/*
!.claude/skills/

# Logs
*.log

# Python cache
__pycache__/

# Contains real temporary passwords — never commit
scripts/leader_accounts_output.csv
scripts/*_accounts_output.csv